project 'AuthenticationUsingFirebase.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AuthenticationUsingFirebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'SwiftKeychainWrapper'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  # Pods for AuthenticationUsingFirebase

  target 'AuthenticationUsingFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
